[pytest]
testpaths = tests
pythonpath = .
markers =
    live_ssh: exercises real SSH when available
    live_tui: exercises real curses/tty when available
    smoke_optional: skip if optional binaries missing
